// Copy to form-config.js (gitignored) and paste your Web3Forms access key.
// Get a free key at https://web3forms.com — delivery email is set there, not on the site.
window.SIPS_FORM_CONFIG = {
  accessKey: "YOUR_WEB3FORMS_ACCESS_KEY",
};
